<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <include
        android:id="@+id/inc_toolbar"
        layout="@layout/toolbar" />

    <android.support.design.widget.TabLayout
        android:layout_below="@+id/inc_toolbar"
        android:id="@+id/my_points_tab"
        style="@style/LivautomationTheme.TabLayout"
        android:background="@color/extract_top_minimized_bg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="?attr/actionBarSize"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar" />

    <android.support.v4.view.ViewPager
        android:layout_below="@+id/my_points_tab"
        android:id="@+id/my_points_pager"
        android:layout_width="match_parent"
        android:layout_height="fill_parent" />


</RelativeLayout>